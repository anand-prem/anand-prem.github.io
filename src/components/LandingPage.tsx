import React, { ReactElement } from 'react';
import { Segment } from 'semantic-ui-react';

export default class LandingPage extends React.Component {
  render(): ReactElement {
    return (
      <Segment>
        <div>Down at the moment!</div>
      </Segment>
    );
  }
}
